#include <iostream>

/*
Class Exercise 1
*/
using namespace std;

int main() {

    // declare variables here
    int area, length, width;

    // next initialize the variables
    length = 8;
    width = 5;

    // now compute the area
    area = length * width;

    // lastly, output the area
    cout << "Area of the rectangle is: " << area << endl;
    return 0;
}